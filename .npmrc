# Skip native module builds for packages we don't need
ignore-scripts=false

# Make npm more lenient with peer dependencies
legacy-peer-deps=true

# Allow installation to continue even if optional dependencies fail
optional=false

