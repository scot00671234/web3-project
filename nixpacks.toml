[phases.setup]
nixPkgs = [
  "nodejs_20",
  "python3",
  "pkg-config",
  "libusb1",
  "eudev",
  "gcc",
  "gnumake"
]

[phases.install]
cmds = [
  "export PYTHON=$(which python3)",
  "npm install --omit=dev"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"

